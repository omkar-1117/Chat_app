PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.dgipboh.mongodb.net/chatapp?retryWrites=true&w=majority
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
CLIENT_URL=http://localhost:3000
NODE_ENV=development

# Atlas tips:
# 1. Replace <username> and <password> with your Atlas database user credentials.
# 2. URL-encode special characters in the password, for example @ becomes %40.
# 3. In Atlas, allow your current IP in Network Access before starting the server.
